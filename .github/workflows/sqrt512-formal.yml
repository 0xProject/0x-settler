name: 512Math sqrt Formal Check

on:
  push:
    branches:
      - master
    paths:
      - src/utils/512Math.sol
      - src/vendor/Sqrt.sol
      - src/wrappers/Sqrt512Wrapper.sol
      - formal/sqrt/**
      - formal/yul/**
      - .github/workflows/sqrt512-formal.yml
  pull_request:
    paths:
      - src/utils/512Math.sol
      - src/vendor/Sqrt.sol
      - src/wrappers/Sqrt512Wrapper.sol
      - formal/sqrt/**
      - formal/yul/**
      - .github/workflows/sqrt512-formal.yml

jobs:
  sqrt512-formal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Install pinned Lean toolchain
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/917c18d0ad52f649c2603dc8b973f5b9fa5f8f43/elan-init.sh -sSf | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          LEAN_TOOLCHAIN="$(cat formal/sqrt/Sqrt512Proof/lean-toolchain)"
          test "$LEAN_TOOLCHAIN" = "$(cat formal/sqrt/SqrtProof/lean-toolchain)"
          "$HOME/.elan/bin/elan" toolchain install "$LEAN_TOOLCHAIN"
          "$HOME/.elan/bin/elan" default "$LEAN_TOOLCHAIN"

      - name: Restore Lean build cache
        uses: actions/cache@v4
        with:
          path: |
            formal/yul/.lake/build
            formal/yul/.lake/packages/*/.lake/build
            lib/EVMYulLean/.lake/build
            formal/sqrt/SqrtProof/.lake/build
            formal/sqrt/Sqrt512Proof/.lake/build
          key: ${{ runner.os }}-sqrt512-formal-lean-${{ hashFiles('formal/yul/lean-toolchain', 'formal/yul/lakefile.toml', 'formal/yul/lake-manifest.json', 'formal/sqrt/SqrtProof/lakefile.toml', 'formal/sqrt/SqrtProof/lake-manifest.json', 'formal/sqrt/Sqrt512Proof/lakefile.toml', 'formal/sqrt/Sqrt512Proof/lake-manifest.json', 'formal/yul/FormalYul/**/*.lean', 'formal/sqrt/SqrtProof/**/*.lean', 'formal/sqrt/Sqrt512Proof/**/*.lean', 'lib/EVMYulLean/**/*.lean') }}
          restore-keys: |
            ${{ runner.os }}-sqrt512-formal-lean-
            ${{ runner.os }}-formal-lean-

      - name: Install solc 0.8.34
        run: forge build -- src/chains/Mainnet/TakerSubmitted.sol
        env:
          FOUNDRY_SOLC_VERSION: 0.8.34

      - name: Fetch Mathlib cache
        working-directory: formal/yul
        run: lake exe cache get

      - name: Build Yul importer
        working-directory: formal/yul
        run: lake build FormalYul.Preservation yul_importer

      - name: Generate 512-bit EVMYulLean artifacts from compiled Sqrt512Wrapper Yul IR
        run: |
          ./formal/yul/generate_from_forge.sh \
            sqrt512 \
            src/wrappers/Sqrt512Wrapper.sol:Sqrt512Wrapper \
            formal/sqrt/Sqrt512Proof/Sqrt512Proof/Sqrt512Yul.lean \
            0.8.34

      - name: Fetch proof dependency cache
        working-directory: formal/sqrt/Sqrt512Proof
        run: lake exe cache get

      - name: Build Sqrt512 proof package
        working-directory: formal/sqrt/Sqrt512Proof
        run: lake build
