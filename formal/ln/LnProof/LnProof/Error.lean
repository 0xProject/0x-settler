/-!
# Error facade

The model-level `1.6986`-ulp error bound. `Bound` is the published cut
statement (`lnWadToRayBody_error_bound_1_6986`); the rest of the `Error/`
directory holds the cell covers, factored-octave caps, and certificate
bridges that discharge it. The runtime transport is `LnProof.ErrorBoundRuntime`.
-/
import LnProof.Error.Bound
